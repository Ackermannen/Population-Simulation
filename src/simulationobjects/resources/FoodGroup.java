package simulationobjects.resources;

import simulationobjects.location.Location;

public class FoodGroup extends ResourceGroup {

    public FoodGroup(float quantity, ResourceType resource, Location location) {
        super(quantity, resource, location);
    }
}
