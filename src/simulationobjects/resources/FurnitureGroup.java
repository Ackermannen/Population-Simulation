package simulationobjects.resources;

import simulationobjects.location.Location;

public class FurnitureGroup extends ResourceGroup {

    FurnitureGroup(float quantity, ResourceType resource, Location location) {
        super(quantity, resource, location);
    }
}
