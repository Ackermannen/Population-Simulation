package simulationobjects.resources;

import simulationobjects.location.Location;

public class ClothesGroup extends ResourceGroup {
    ClothesGroup(float quantity, ResourceType resource, Location location) {
        super(quantity, resource, location);
    }
}
